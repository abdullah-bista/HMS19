<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_hms_grant_portal_access_form" model="ir.ui.view">
        <field name="name">hms.grant.portal.access.form</field>
        <field name="model">hms.grant.portal.access</field>
        <field name="arch" type="xml">
            <form string="Grant Portal Access">
                <group>
                    <field name="patient_id"/>
                    <field name="partner_id"/>
                    <field name="email"/>
                </group>
                <footer>
                    <button name="action_grant_access" type="object"
                            string="Grant Access" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="action_hms_grant_portal_access" model="ir.actions.act_window">
        <field name="name">Grant Portal Access</field>
        <field name="res_model">hms.grant.portal.access</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="context">{'default_patient_id': active_id}</field>
    </record>

</odoo>
