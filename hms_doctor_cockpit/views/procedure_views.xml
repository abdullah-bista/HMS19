<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- My Procedures: filtered to current physician -->
    <record id="action_my_procedures" model="ir.actions.act_window">
        <field name="name">My Procedures</field>
        <field name="res_model">acs.patient.procedure</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[('physician_id.user_id', '=', uid)]</field>
        <field name="view_id" ref="acs_hms.view_acs_patient_procedure_list"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">No procedures found.</p>
            <p>Procedures assigned to you will appear here.</p>
        </field>
    </record>

</odoo>
