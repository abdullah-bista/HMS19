<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_report_discharge_summary" model="ir.actions.report">
        <field name="name">Discharge Summary</field>
        <field name="model">hms.discharge</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">bista_hms_emergency.report_discharge_summary_template</field>
        <field name="report_file">bista_hms_emergency.report_discharge_summary_template</field>
        <field name="print_report_name">'Discharge Summary - %s' % (object.name)</field>
        <field name="binding_model_id" ref="model_hms_discharge"/>
        <field name="binding_type">report</field>
    </record>

</odoo>
